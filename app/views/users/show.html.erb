<h1>User: <%= @user.email %></h1>
<h3><%= @user.email %>'s events: </h3>

<%= render @user.events %>

<h3><%= @user.email %>'s attended events: </h3>

<ul>
  <% @user.attended_events.each do |event| %>
    <li>
      <%= link_to event.title, event_path(event) %>
    </li>
  <% end %>
</ul>
